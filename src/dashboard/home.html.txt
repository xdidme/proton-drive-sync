<div class="max-w-7xl mx-auto space-y-8">
  <!-- Stats Overview -->
  <div id="stats" hx-ext="morph" hx-swap="morph:innerHTML" sse-swap="stats" hx-swap-oob="morph:innerHTML">
    {{STATS_CONTENT}}
  </div>

  <!-- Config Info -->
  <div class="bg-gray-800/50 border border-gray-700/50 rounded-lg p-4 shadow-sm">
    <h2 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Tracked Paths</h2>
    <div id="config-info" hx-swap="innerHTML">{{CONFIG_INFO_CONTENT}}</div>
  </div>

  <!-- Main Content Grid -->
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <!-- Pending Transfers -->
    <div class="bg-gray-800 rounded-xl border border-gray-700 flex flex-col h-[400px]">
      <div
        id="pending-queue"
        class="flex flex-col h-full"
        hx-ext="morph"
        hx-swap="morph:innerHTML"
        sse-swap="pending-queue"
      >
        {{PENDING_QUEUE_CONTENT}}
      </div>
    </div>

    <!-- Active Processing -->
    <div class="bg-gray-800 rounded-xl border border-gray-700 flex flex-col h-[400px]">
      <div
        id="processing-queue"
        class="flex flex-col h-full"
        hx-ext="morph"
        hx-swap="morph:innerHTML"
        sse-swap="processing-queue"
      >
        {{PROCESSING_QUEUE_CONTENT}}
      </div>
    </div>
  </div>

  <!-- Queue Overview: 3 columns -->
  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Recently Synced -->
    <div class="bg-gray-800 rounded-xl border border-gray-700 flex flex-col h-[300px]">
      <div
        id="recent-queue"
        class="flex flex-col h-full"
        hx-ext="morph"
        hx-swap="morph:innerHTML"
        sse-swap="recent-queue"
      >
        {{RECENT_QUEUE_CONTENT}}
      </div>
    </div>

    <!-- Scheduled Retries -->
    <div class="bg-gray-800 rounded-xl border border-gray-700 flex flex-col h-[300px]">
      <div id="retry-queue" class="flex flex-col h-full" hx-ext="morph" hx-swap="morph:innerHTML" sse-swap="retry-queue">
        {{RETRY_QUEUE_CONTENT}}
      </div>
    </div>

    <!-- Blocked Jobs -->
    <div class="bg-gray-800 rounded-xl border border-gray-700 flex flex-col h-[300px]">
      <div
        id="blocked-queue"
        class="flex flex-col h-full"
        hx-ext="morph"
        hx-swap="morph:innerHTML"
        sse-swap="blocked-queue"
      >
        {{BLOCKED_QUEUE_CONTENT}}
      </div>
    </div>
  </div>

  <!-- Logs (Collapsible) -->
  <div class="bg-gray-800 rounded-xl border border-gray-700 flex flex-col">
    <div
      id="logs-header"
      class="px-5 py-4 flex justify-between items-center bg-gray-800/50 backdrop-blur rounded-t-xl rounded-b-xl"
    >
      <button
        onclick="toggleLogs()"
        class="text-sm font-semibold text-gray-100 uppercase tracking-wider flex items-center gap-2 cursor-pointer hover:text-white transition-colors"
      >
        {{ICON_ALIGN_LEFT}}
        Live Logs
        {{ICON_CHEVRON_RIGHT}}
      </button>
      <div id="logs-controls" class="hidden flex items-center gap-2">
        <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
        <!-- Log Level Filter -->
        <div class="flex items-center gap-2 ml-4">
          <span class="text-[10px] text-gray-500 font-medium uppercase tracking-wider"
            >Min Level</span
          >
          <div class="flex bg-gray-900 rounded-lg p-0.5 border border-gray-700">
            <button
              onclick="setLogLevel(20)"
              id="log-level-20"
              class="px-2 py-0.5 text-[10px] font-medium rounded transition-all duration-200 bg-gray-700 text-gray-200 shadow-sm"
            >
              DBG
            </button>
            <button
              onclick="setLogLevel(30)"
              id="log-level-30"
              class="px-2 py-0.5 text-[10px] font-medium rounded transition-all duration-200 text-gray-500 hover:text-gray-400 hover:bg-gray-800/50"
            >
              INF
            </button>
            <button
              onclick="setLogLevel(40)"
              id="log-level-40"
              class="px-2 py-0.5 text-[10px] font-medium rounded transition-all duration-200 text-gray-500 hover:text-gray-400 hover:bg-gray-800/50"
            >
              WRN
            </button>
            <button
              onclick="setLogLevel(50)"
              id="log-level-50"
              class="px-2 py-0.5 text-[10px] font-medium rounded transition-all duration-200 text-gray-500 hover:text-gray-400 hover:bg-gray-800/50"
            >
              ERR
            </button>
          </div>
        </div>
      </div>
    </div>
    <div
      id="logs-container"
      class="hidden overflow-y-auto custom-scrollbar p-4 bg-gray-950 font-mono text-xs leading-relaxed h-[356px] rounded-b-xl"
      hx-ext="sse"
      sse-connect="/api/logs"
      sse-swap="log"
      hx-swap="beforeend scroll:bottom"
    >
      <div class="text-gray-600 italic">Waiting for logs...</div>
    </div>
  </div>
</div>
